---

let { title, img, alt, eyebrow, details, url } = Astro.props;

---


<div class="font-harmonia text-left rounded-lg border-gray-50 border-2 shadow-md hover:shadow-lg lg:w-1/4 md:w-1/3 sm:w-1/2 m-2">
    <img class="rounded-lg shadow-inner" src={ img } alt={ alt }>
    <div class="mt-2 p-5">
        <div>
            <div class="text-xs text-slate-600 uppercase font-bold tracking-wider">{ eyebrow }</div>
            <div class="h-2"></div>
            <div class="font-bold text-slate-700 leading-snug">
                <a href={ url } class="hover:underline gradient-text">{ title }</a>
            </div>
            <div class="mt-2 text-sm text-slate-600 font-harmonia-regular">{ details }</div>
        </div>
    </div>
</div>

  
<script>
    export default {
        props: ['img', 'alt', 'eyebrow', 'title', 'details', 'url']
    }
 </script>